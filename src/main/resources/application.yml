spring:
  cloud:
    stream:
      default-binder: kafka
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        input:
          binder: kafka
          destination: INVENTORY_NEW_ORDER
          content-type: application/json
          group: inventory-messaging-group